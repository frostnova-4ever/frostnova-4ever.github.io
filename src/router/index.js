import { createRouter, createWebHistory } from "vue-router";
import index from "../components/index.vue";
import writer from "../components/admin/writer.vue";

const routes = [{
    path: '/',
    name: 'index',
    component: index
}, ]